source 'https://rubygems.org'

puts "RUN bin/install to install system libs\n\n" if @inferred_plugins

gem 'rack'
gem 'json'
gem 'dotenv'
gem 'sinatra'

group :development do
  gem 'awesome_print'
  gem 'puma'
  gem 'colorize'
  gem 'rerun'
  gem 'pry'
end

group :test do
  gem 'rspec'
end
